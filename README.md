
# 🎬 Chat Online & Watch Together

![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.0-green)
![Docker](https://img.shields.io/badge/Docker-Compose-blue)
![Kafka](https://img.shields.io/badge/Apache%20Kafka-Enabled-red)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)

**Chat Online** — это современная платформа для общения и совместного просмотра видео в реальном времени. Проект построен на микросервисной архитектуре (с использованием Kafka) и упакован в Docker для легкого развертывания.

Главная особенность — **комнаты синхронного просмотра**, где действия одного пользователя (пауза, перемотка) мгновенно транслируются всем участникам.

---

## 🔥 Ключевая фича: Совместный просмотр (Watch Together)

Реализована система синхронизации видеоплеера с точностью до миллисекунд. Вы можете смотреть фильмы с друзьями, находясь в разных точках мира.

### Как это работает?
1.  **Стриминг:** Видеопоток передается с облачного хранилища (Google Drive / S3) напрямую в браузер через API.
2.  **Синхронизация:** Используется протокол **WebSocket (STOMP)**.
3.  **События:**
    *   Когда хост нажимает `PLAY`, `PAUSE` или делает `SEEK` (перемотку), событие отправляется на сервер.
    *   Сервер через брокер сообщений рассылает сигнал всем подписчикам комнаты в топик `/topic/video/{roomId}`.
    *   Клиенты получают сигнал и плеер автоматически подстраивается под время хоста.
4.  **Защита от "Эха":** Реализован алгоритм, предотвращающий зацикливание событий (когда ваш плеер получает ваш же сигнал).

---

## 🛠 Технологический стек

### Backend
*   **Java 21 & Spring Boot 3** — ядро приложения.
*   **Spring Security** — авторизация и защита.
*   **Spring Data JPA (Hibernate)** — работа с базой данных.
*   **Apache Kafka (KRaft mode)** — брокер сообщений для масштабируемости чата.
*   **WebSocket (SockJS + STOMP)** — двусторонняя связь для чата и видео.

### Frontend
*   **Thymeleaf** — серверный рендеринг шаблонов.
*   **Bootstrap 5** — адаптивный UI (Mobile First).
*   **Video.js** — кастомизируемый HTML5 плеер.
*   **JavaScript (ES6)** — логика синхронизации на клиенте.

### DevOps & Infrastructure
*   **Docker & Docker Compose** — оркестрация контейнеров.
*   **PostgreSQL 16** — основная база данных.
*   **Nginx Proxy Manager** — SSL-терминация, Reverse Proxy и управление доменами.

---

## 🚀 Как запустить проект

Для запуска вам понадобятся только **Docker** и **Git**.

### 1. Клонирование репозитория
```bash
git clone https://github.com/Kirill-Makarov-genius/chat-online.git
cd chat-online
```

### 2. Настройка окружения
Создайте файл `.env` в корне проекта и заполните его своими данными:

```env
# База данных
DB_PASSWORD=my_secure_password
POSTGRES_USER=postgres
POSTGRES_DB=chat-online-db

# Почта (для уведомлений)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password

# Kafka (внутренняя настройка)
KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
```

### 3. Запуск через Docker Compose
Проект запустит 4 контейнера: App, Postgres, Kafka, Nginx Proxy Manager.

```bash
docker compose up -d --build
```

После запуска приложение будет доступно через Nginx.

---

## ⚙️ Настройка Nginx Proxy Manager

Чтобы чат и видео работали корректно (особенно на мобильных устройствах), необходима правильная настройка прокси.

1.  Зайдите в админку NPM (порт `81`).
2.  Создайте `Proxy Host` для вашего домена.
3.  Направьте его на контейнер `chat_app` порт `8080`.
4.  **Обязательно** включите галочку **Websockets Support**.
5.  Во вкладке **Advanced** добавьте настройки для корректного стриминга видео:

```nginx
location / {
    proxy_pass http://chat_app:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Отключение буферизации для видео-стриминга
    proxy_buffering off;
    proxy_request_buffering off;
    
    # Поддержка WebSockets
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

---

## 📱 Мобильная адаптация

Интерфейс полностью адаптирован под мобильные устройства:
*   Использование `100dvh` для корректного отображения на iOS/Android.
*   Скрытие сайдбара/чата в зависимости от контекста.
*   Поддержка тач-событий в плеере.

---

## 🤝 Контрибьютинг

Буду рад любым Pull Request'ам и Issue. Особенно интересны предложения по улучшению алгоритма синхронизации видео при плохом интернет-соединении и адаптация под мобилку.
